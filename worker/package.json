{
  "name": "baking-log-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply baking-log --local",
    "db:migrate:remote": "wrangler d1 migrations apply baking-log --remote --yes",
    "db:migrate:create": "wrangler d1 migrations create baking-log"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "wrangler": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
