{
  "name": "baking-log-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply baking-log --local",
    "db:migrate:remote": "wrangler d1 migrations apply baking-log --remote",
    "db:migrate:create": "wrangler d1 migrations create baking-log"
  },
  "dependencies": {
    "hono": "4.13.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "wrangler": "^4.125.0",
    "typescript": "^5.7.0"
  }
}
